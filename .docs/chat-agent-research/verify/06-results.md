# 第 6 章主張驗證結果（T6 使用者模擬與回饋）

驗證對象：`.docs/chat-agent-research/chapters/06-user-simulator-feedback.md`。

四條候選主張都能用程式驗，所以四條都驗了。

- 每條一支程式，只用 Python 標準函式庫，沒有隨機數。
- 最慢的一支約 3.6 秒，其餘都在 0.1 秒內。
- 輸入數字全部由程式從 `.cache/text/<id>.txt` 直接解析，不手抄；主張原文取自 `notes/<id>.json`。行號寫在各程式的檔頭與輸出裡。

一條主張裡如果有幾個子主張的判讀不同，就分開下結論。

## 總表

| # | 子主張 | 結論 |
| --- | --- | --- |
| 1a | GenTUS 訓練的 DS 列平均約 0.55，三者最低；ABUS-T 約 0.64 | **證實**（0.5467／0.6367）。不含對角線時 GenTUS 仍最低，但只比 ABUS-T 低 0.01 |
| 1b | 模擬端的列平均排序與真人相反，交叉模型評估預測不了真人排名 | **證實**（Kendall τ = −1）。但三組兩兩比較裡，ABUS-S 對 ABUS-T 的真人差距不顯著 |
| 1c | 章節收窄版：以 ABUS-T 測、以 ABUS-S 測兩欄與真人完全相反，以 GenTUS 測那欄不相反 | **證實**。GenTUS 那欄 τ = +1/3：不相反，但也不一致 |
| 2a | 隨機基準 1/6；標準誤 1.18；20.61、20.43 超過 3 SE；19.17 約 2.1 SE；17.48、17.47 不顯著 | **在 n=1000、彼此獨立的假設下證實** |
| 2b | 五格中三格高於隨機（未校正） | **證實**。Holm 與 Bonferroni 校正後剩兩格 |
| 2c | 筆記：「Agenda 的 16.9–19.5 等於隨機」 | **部分推翻**。範圍只涵蓋 MultiWOZ；其中 T_D 19.47 已高於隨機；JDDC 兩格顯著低於隨機 |
| 2d | ED 表沒做顯著性檢定 | **證實** |
| 2e | 筆記：「全文沒做顯著性檢定」 | **推翻**。Table 2、3、7、8、12 有 t-test 或顯著性標記 |
| 2f | 每格 n=1000 的前提 | **無法判定**。三個模擬器的 24 個數值乘 10 全都不是整數，與 1000 組 0／1 的平均不相容 |
| 3a | π ≤ P/R，逐列取最小值得到最緊上限約 0.38 | **證實**（Uncertainty Gap 0.38；把捨入算進去是 0.387） |
| 3b | 筆記：「可推知『不滿意』約佔 38%–39%」 | **推翻**：這只是上限。π 的實際值**無法判定**，整數列舉的可行範圍是 0.153–0.385 |
| 3c | 筆記：「0.56 的 F1 已接近平凡解的水準」 | **無法判定**：只在 π 貼近上限時成立 |
| 3d | 章節：「勝過平凡解多少無法從表中確定」 | **證實** |
| 3e | Table 4 的比較條件不對等（事後判斷對事前預測） | **證實**（文字證據，不是算出來的） |
| 4a | 筆記：N2 對 A1 的 z 約 1.4；N1 對 A1、N2 對 N1 的 z 約 0.7 | **證實** |
| 4b | 筆記：A1 對 A2 的 z 約 0.7 | **推翻**（實為 0.54）。章節的更正（0.000767／2.77／0.54）**證實** |
| 4c | 章節：最大的一對 N2 對 A2，z ≈ 1.91，仍然 p>0.05 | **照表面值證實**（z = 1.917，p = 0.055）。結論對取整方式與檢定方法敏感 |
| 4d-i | 六對全部未達雙尾 0.05：照表面值（n=250），以及 n=250 的 Fisher | **證實** |
| 4d-ii | 六對全部未達雙尾 0.05：在所有與論文相容的場數與取整下 | **無法判定**。N2 對 A2 在相容的組合裡，有的 p < 0.05（最低 0.036），有的 > 0.05（表面值 0.055），落在 0.05 兩側 |
| 4e | 論文沒做顯著性檢定 | **證實**：全文沒有 significan、p-value、t-test 字樣 |

---

## 1. GenTUS：模擬評估與真人評估的排名互相矛盾

**主張。** 筆記版：Table 5 中 GenTUS 訓練的 DS 列平均 success 約 0.55（0.68／0.43／0.53），是三者最低，ABUS-T 訓練的約 0.64；Table 6 的真人試驗卻是 GenTUS 訓練的 DS 最高（0.86 對 0.75／0.79）。所以交叉模型的模擬評估預測不了真人排名。

章節收窄版（第 154、227 行）：

- 以 ABUS-T 測的那一欄（0.78／0.74／0.68）與以 ABUS-S 測的那一欄（0.63／0.57／0.43），順序都與真人完全相反。
- 只有以 GenTUS 測的那一欄（0.53 對 0.50）不相反。

**出處。** [arXiv:2208.10817]，`notes/2208.10817.json` 的 limitations_observed 第 1 條。

**輸入。** 都取自 `.cache/text/2208.10817.txt`：

| 行數 | 內容 |
| --- | --- |
| 第 428–461 行 | Table 5：3×3 的 success 矩陣。caption 寫每格 400 段 × 5 個 seed，共 2K 段 |
| 第 430–432 行 | 正文的三個差值：15%、28%、17% |
| 第 464–482 行 | Table 6：三個真人 success，每系統 300 場 |
| 第 250 行 | 真人端每種模擬器只挑「在自己訓練模擬器上最好」的那個策略 |
| 第 519 行 | 作者說 ABUS-T 與 ABUS-S 的真人差異不顯著 |

**方法。**

1. 先用正文三個差值釘死方向：「列＝訓練、欄＝測試」三個都對得上，轉置後對不上。
2. 列平均：含對角線與不含對角線各算一次。
3. 逐欄排序，與真人排序比對，算 Kendall τ。
4. 補充：把真人端（n=300）與模擬端（n=2000）各自當成二項樣本，做兩比例 z 檢定。模擬端忽略 seed 間變異，所以算出的 z 是上限。

**實際輸出（摘要）。**

- 方向：0.68 − 0.53 = 0.15、0.78 − 0.50 = 0.28、0.74 − 0.57 = 0.17，與正文完全一致。
- 列平均：

  | 訓練用模擬器 | 含對角 | 不含對角 |
  | --- | --- | --- |
  | ABUS-T | 0.6367 | 0.5650 |
  | ABUS-S | 0.5867 | 0.5950 |
  | GenTUS | **0.5467** | **0.5550** |

- 真人排序是 GenTUS > ABUS-S > ABUS-T。各欄與它比對：

  | 排序依據 | 模擬端順序 | τ |
  | --- | --- | --- |
  | 以 ABUS-T 測 | ABUS-T > ABUS-S > GenTUS | −1 |
  | 以 ABUS-S 測 | ABUS-T > ABUS-S > GenTUS | −1 |
  | 以 GenTUS 測 | GenTUS > ABUS-T > ABUS-S | +1/3 |
  | 列平均（含對角） | ABUS-T > ABUS-S > GenTUS | −1 |
  | 列平均（不含對角） | ABUS-S > ABUS-T > GenTUS | −1/3 |

- 真人端的雙尾 p：

  | 比較 | p |
  | --- | --- |
  | GenTUS 對 ABUS-S | 0.024 |
  | GenTUS 對 ABUS-T | 0.0007 |
  | ABUS-S 對 ABUS-T | 0.244（不顯著） |

- 模擬端以 GenTUS 測時，GenTUS 0.53 對 ABUS-T 0.50 的 z = 1.90，p = 0.058。即使忽略 seed 間變異也在邊界上。

**結論。**

- 筆記的數字與「預測不了真人排名」**證實**。
- 章節的收窄說法**證實**。以 GenTUS 測那一欄確實「不相反」，但 ABUS-T 與 ABUS-S 的位置對調，所以也不是「一致」。
- 每欄的三組兩兩比較裡，GenTUS 對兩個 ABUS 的兩組在兩端都顯著，這個反向撐得住。
- ABUS-S 對 ABUS-T 那一組，真人端的差距不顯著，作者自己也這麼說。所以「完全相反」裡的這一組不能當證據。
- 兩端比的東西也不同：模擬端是 5 個 seed 的平均，真人端是每種模擬器挑出的單一最佳策略（第 250 行）。

**重現。** `python3 .docs/chat-agent-research/verify/06-gentus-sim-vs-human-rank.py`

---

## 2. MetaSim：ED 在 MultiWOZ 以外的鑑別力

**主張。** 筆記版：三個變體全部排對的隨機機率約 1/6 ≈ 16.7%，Agenda 的 16.9–19.5 等於隨機；MetaSim 在 ReDial（17.48／20.61／20.43）與 JDDC（19.17／17.47）只比隨機高 1–4 個百分點，而且全文沒做顯著性檢定。

章節修正版（第 195 行）：假設 1K 組偏好彼此獨立、沒有平手，標準誤約 1.18 個百分點。

- 20.61 與 20.43 超過 3 個標準誤；19.17 約 2.1 個；17.48 與 17.47 不顯著。
- 所以五格中三格高於隨機（未做多重比較校正），但幅度都在 4 個百分點以內。

**出處。** [arXiv:2204.00763]，`notes/2204.00763.json` 的 limitations_observed 第 2 條。

**輸入。** 都取自 `.cache/text/2204.00763.txt`：

| 行數 | 內容 |
| --- | --- |
| 第 1157–1214 行 | Table 4 的 ED 表。caption 在表格之前 |
| 第 1224–1227 行 | 同分時以輪數少者為佳；每組偏好記 0／1；在 1K 組偏好上取平均 |
| 第 774、883、1505、1650、2029 行 | 其他表格的 t-test／顯著性標記 |

**方法。**

1. 檢查每格乘以 10 是不是整數。1K 組 0／1 的平均只能是 0.1% 的倍數。
2. 虛無假設下，六種排序機率相等，p0 = 1/6，n = 1000。
3. 成功次數 = ED% × 10，不是整數，所以 floor 與 ceil 兩個 k 都算精確二項尾機率（`math.comb` 整數運算）。高於 1/6 的格算上尾，低於的算下尾。
4. MetaSim 的五格分別做未校正、Bonferroni、Holm 三種判定（單尾，取兩個 k 中較保守的 p）。
5. 敏感度：算出 17.48 與 17.47 要多大的有效 n 才會達到單尾 0.05。
6. 檢查 ED 表區塊裡有沒有 `*` 或 signific，並列出全文其他做了檢定的地方。

**實際輸出（摘要）。**

- 三個模擬器的 24 個數值乘以 10 **全都不是整數**，例如 17.48 → 174.8 次。Human 列來自 40 位標註者、不是 1K 組偏好，所以不列入。
- 標準誤 = 1.1785 個百分點。MetaSim 在 ReDial／JDDC 的五格：

  | 格 | ED | z | 單尾 p（保守） | 未校正 | Bonferroni | Holm |
  | --- | --- | --- | --- | --- | --- | --- |
  | ReDial/T_C | 17.48 | +0.69 | 0.279 | 不顯著 | 不顯著 | 不顯著 |
  | ReDial/T_R | 20.61 | +3.35 | 0.00066 | 顯著 | 顯著 | 顯著 |
  | ReDial/T_D | 20.43 | +3.19 | 0.0011 | 顯著 | 顯著 | 顯著 |
  | JDDC/T_C | 19.17 | +2.12 | 0.023 | 顯著 | 不顯著 | 不顯著（門檻 0.0167） |
  | JDDC/T_D | 17.47 | +0.68 | 0.279 | 不顯著 | 不顯著 | 不顯著 |

- 五格高出隨機的幅度是 0.80–3.94 個百分點。
- 17.48 與 17.47 要有效 n ≥ 約 5700 才會達到單尾 0.05。
- Agenda 的八格：

  | 格 | ED | z | 單尾 p | 判讀 |
  | --- | --- | --- | --- | --- |
  | MultiWOZ/T_C、T_R | 16.93、17.28 | +0.22、+0.52 | 0.43、0.34 | 與隨機分不開 |
  | MultiWOZ/T_D | 19.47 | +2.38 | 0.0125 | 高於隨機 |
  | ReDial 三格 | 16.41–17.12 | −0.22 至 +0.38 | 0.37–0.46 | 與隨機分不開 |
  | JDDC/T_C | 13.76 | −2.47 | 0.0074（下尾） | 低於隨機 |
  | JDDC/T_D | 14.52 | −1.82 | 0.042（下尾） | 低於隨機 |

- ED 表區塊沒有 `*`，也沒有 signific。但 Table 2（第 774 行）、Table 3（第 883 行）、Table 7（第 1505 行）、Table 8（第 1650 行）、Table 12（第 2029 行）都有 t-test 或顯著性標記。

**結論。**

- 章節的算式（1/6、1.18、3 SE、2.1 SE）與「五格中三格，未校正」**在 n=1000 的假設下證實**。校正多重比較後只剩 ReDial 的兩格；JDDC/T_C 的 p = 0.023 未達 Holm 門檻 0.0167。
- 筆記的「只高 1–4 個百分點」幅度**證實**。「鑑別力幾乎不存在」對 ReDial 的 T_R、T_D 說得太滿。
- 筆記的「Agenda 16.9–19.5 等於隨機」**部分推翻**：
  - 這個範圍只涵蓋 MultiWOZ，而且其中 T_D 19.47 在同一假設下已經高於隨機。
  - JDDC 兩格顯著低於 1/6，代表系統性地排反，比「等於隨機」更糟。「等於隨機」反而低估了 Agenda 在 JDDC 的問題。
  - 第 1224 行的同分規則（輪數少者為佳）是可能的機制，但論文沒給資料可以驗。
- 「沒做顯著性檢定」：ED 表確實沒做，**證實**；若指全文，**推翻**。
- **所有檢定都是條件式的。** 表上的兩位小數不可能是單一次 1000 組 0／1 的平均，實際樣本數或平均方式論文沒交代，所以 n=1000 這個前提**無法判定**。
  - 有效 n 若比 1000 大（例如多次重複取平均），會有更多格顯著。
  - 同一組偏好若在變體間相關，有效 n 會變小，顯著的格會變少。

**重現。** `python3 .docs/chat-agent-research/verify/06-metasim-ed-binomial.py`

---

## 3. Self-feeding：不確定度基線與平凡解

**主張。** 筆記版：

- Uncertainty Top 在 recall 0.99 時 precision 為 0.39，可推知測試集中「不滿意」約佔 38%–39%。
- 0.56 的 F1 已接近「全部判為錯誤」這個平凡解的水準。
- Table 4 的比較條件不對等，是事後判斷對事前預測。

章節修正版（第 203、270 行）：

- π ≤ P/R，Uncertainty Gap 的 0.38 ÷ 1.00 給出 π 最多約 0.38。
- 平凡解 F1 = 2π／(1+π)，π 取上限時約 0.55。
- 38% 只是上限；論文沒報 π，所以勝過平凡解多少無法從表中確定。

**出處。** [arXiv:1901.05415]，`notes/1901.05415.json` 的 limitations_observed 第 5 條。

**輸入。** 都取自 `.cache/text/1901.05415.txt`：

| 行數 | 內容 |
| --- | --- |
| 第 489–544 行 | Table 4 十列的 P／R／F1 |
| 第 207 行 | Table 1：Satisfaction 測試集 1000 筆 |
| 第 558 行 | Table 4 是在 Satisfaction 測試集上量的 |
| 第 295 行 | rating 1 定為 negative class（dissatisfied），3–5 定為 positive class |
| 第 559、563 行 | 不確定度方法「predict a mistake」；regex 偵測 dissatisfaction |
| 第 288、550、553、561 行 | 事後對事前的文字證據；不確定度用的是 131k 模型 |

**方法。**

1. 上限：TP = R·π·N，TP／P ≤ N，所以 π ≤ P/R。逐列算，再把兩位小數的捨入算進去。
2. 在上限處算平凡解 F1。
3. 整數列舉：N = 1000。對每個 Npos，逐列找整數 TP 與預測為正例的筆數 PP，使捨入後的 P、R、F1 都等於表列值。十列共用同一個測試集，所以取交集。
4. 附條件的第二條上限：若門檻掃描包含「全判為正例」，則 π ≤ F1／(2−F1)。
5. 列出文字證據。

**實際輸出（摘要）。**

- 逐列 P/R 的最小值是 **Uncertainty Gap 的 0.3800**。把捨入算進去是 0.3869。筆記用的 Top 是 0.39／0.99 = 0.3939，比較鬆。
- π = 0.38 時平凡解 F1 = 0.5507。Gap 表列 0.55，與它相差 −0.0007；Top 表列 0.56，高 0.0093。
- 整數列舉：十列交集有 195 個 Npos，**從 153 到 385**（π 約 0.153–0.385）。250 以上每個 Npos 都可行；150–249 較稀疏（100 個裡有 59 個可行），但一路到 153 都還有解。

  | Npos（π） | Gap 列的解 | 平凡解 F1 | Top 0.56 高出 |
  | --- | --- | --- | --- |
  | 153（0.153） | TP=153、PP=399 | 0.265 | +0.295 |
  | 288（0.288） | TP=287、PP=747 | 0.447 | +0.113 |
  | 385（0.385） | TP=384、PP=999 | 0.556 | +0.004 |

  π 取上限時，Gap 那一列幾乎就是全判為正例（PP=999）。
- 附條件的上限：Gap 給 π ≤ 0.3841，Top 給 π ≤ 0.3937。這要求門檻掃描包含「全判為正例」，論文沒寫。
- 文字證據：
  - 第 288 行：看使用者對回應的下一句話，判斷會容易得多。
  - 第 550 行：認出已經犯下的錯，比事先避免犯錯容易。
  - 第 553 行：不確定度基線是在 bot 開口前、自己最不確定時要回饋。

**結論。**

- 上限算式與「最緊約 0.38」**證實**。
- 筆記「可推知約佔 38%–39%」**推翻**：表格只給得出上限，π 可以低到 0.15 左右。π 的實際值**無法判定**。
- 「0.56 已接近平凡解」只在 π 貼近上限時成立，所以**無法判定**。π 越小，平凡解 F1 越低，Top 勝過平凡解的幅度最多可到約 0.29。
- 章節「勝過平凡解多少無法從表中確定」**證實**。
- 「事後判斷對事前預測」**證實**。這是文字證據，不是算出來的。
- 假設：論文第 295 行把 satisfied 定為 positive class。但 Table 4 的方法都在偵測錯誤或不滿，所以筆記把 P/R 讀成針對「不滿意」，這是推論。上限算式對「被偵測的那一類」都成立。

**重現。** `python3 .docs/chat-agent-research/verify/06-selffeeding-prevalence-bound.py`

---

## 4. NUS：真人評估差距的顯著性

**主張。** 筆記版：每個策略只有 250 場，差距沒做顯著性檢定。依樣本數推算多半不顯著：

- N2 對 A1 差 3.4 個百分點，標準誤約 2.5，z 約 1.4。
- N1 對 A1、N2 對 N1、A1 對 A2 的 z 都約 0.7。
- 都達不到 p<0.05。

章節修正版（第 194 行）用不合併變異數重算：

- A1 對 A2：變異數 ≈ 0.000767，z ≈ 1.5／2.77 ≈ 0.54，比筆記低。
- 最大的一對 N2 對 A2：變異數 ≈ 0.000654，z ≈ 4.9／2.56 ≈ 1.91，仍未達雙尾 0.05。

**出處。** [arXiv:1805.06966]，`notes/1805.06966.json` 的 limitations_observed 第 1 條。

**輸入。** 都取自 `.cache/text/1805.06966.txt`：

| 行數 | 內容 |
| --- | --- |
| 第 410–441 行 | Table 4：N2 93.4、N1 91.8、A1 90.0、A2 88.5 |
| 第 312 行 | 1000 場，每個策略 250 場 |
| 第 313 行 | 受試者隨機分派到四個系統 |
| 第 412 行 | 每個策略 250 場 |

**方法。**

1. 換算成功次數。93.4、91.8、**88.5** 乘以 250 都不是整數（233.5、229.5、221.25；任務沒點到 88.5 這一格），只有 90.0 是整數（225）。所以三格各取 floor／ceil，共 8 種組合。
2. 六對都做三種雙尾檢定：兩比例 z 檢定（合併變異數）、兩比例 z 檢定（不合併變異數）、Fisher 精確檢定。
3. 表面值版：直接用表列比例、n=250，重現筆記與章節的算式。
4. N 掃描：受試者是隨機分派，每個策略未必剛好 250 場。在 N∈[200,300] 與 N∈[240,260] 兩個範圍內，找出所有能重現表列百分比的 (N, k)，並要求四個 N 加總為 1000。

**實際輸出（摘要）。**

- 全文沒有 significan、p-value、t-test 字樣。
- 表面值版（n=250）：

  | 對 | 差（點） | z（不合併） | z（合併） | 雙尾 p |
  | --- | --- | --- | --- | --- |
  | N2 對 N1 | 1.6 | 0.684 | 0.683 | 0.494 |
  | N2 對 A1 | 3.4 | 1.380（SE 2.46） | 1.378 | 0.167 |
  | N2 對 A2 | 4.9 | **1.917**（SE 2.56） | 1.910 | **0.055** |
  | N1 對 A1 | 1.8 | 0.700 | 0.700 | 0.484 |
  | N1 對 A2 | 3.3 | 1.240 | 1.238 | 0.215 |
  | A1 對 A2 | 1.5 | **0.542**（SE 2.77） | 0.541 | 0.588 |

- n=250 的 8 種取整組合裡，各對的最小雙尾 p：

  | 對 | z 合併 | z 不合併 | Fisher |
  | --- | --- | --- | --- |
  | N2 對 A2 | **0.042** | **0.041** | 0.060 |
  | 其他五對 | ≥ 0.14 | ≥ 0.14 | ≥ 0.19 |

  跨過 0.05 的只有一種組合：N2 取 234/250、A2 取 221/250。而 234/250 = 93.6%，本身就重現不了表列的 93.4%。
- N 掃描：四個策略裡只有 A1 能在 N=250 時重現表列值。N2 對 A2 的結果：

  | 範圍 | 可行組合 | 最小 p（z 合併） | 最小 p（z 不合併） | 最小 p（Fisher） |
  | --- | --- | --- | --- | --- |
  | [240,260] | 22 | 0.0515 | 0.0496（1 個組合） | 0.0607 |
  | [200,300] | 583 | 0.037（160 個組合 < 0.05） | 0.036（181 個） | 0.041（23 個） |

  其他五對在兩個範圍內全部 ≥ 0.13。

**結論。**

- 筆記點名的四對（N2–A1、N1–A1、N2–N1、A1–A2）不顯著：在所有取整方式與 N 掃描下都**證實**，最小 p 都大於 0.13。
- 筆記的 z 值：N2 對 A1 約 1.4、N1 對 A1 與 N2 對 N1 約 0.7，**證實**。A1 對 A2「約 0.7」**推翻**，實為 0.54。
- 章節的算式（0.000767／2.77／0.54 與 0.000654／2.56／1.91）**全部重現**。
- 「六對全部未達雙尾 0.05」照表面值與 n=250 的 Fisher **證實**（4d-i）。放寬到所有與論文相容的場數與取整則**無法判定**（4d-ii），因為 N2 對 A2 是邊界：
  - z 檢定在極端取整下會跨過 0.05。
  - 在 N∈[240,260] 能重現表列值的組合裡，只有 1／22 以 p=0.0496 擦邊跨過。
  - 放寬到 N∈[200,300] 時，Fisher 也有組合跨過。
  - 建議章節把「仍然 p>0.05」寫成「p 約 0.055，落在邊界」。
- 附帶發現：93.4、91.8、88.5 都不可能由剛好 250 場得到。「每個策略 250 場」與表列成功率對不上，每個策略實際跑了幾場，論文沒交代。

**重現。** `python3 .docs/chat-agent-research/verify/06-nus-human-significance.py`
