# T7 章節主張的程式驗證結果

章節：[`chapters/07-agent-evaluation.md`](../chapters/07-agent-evaluation.md)。四條候選主張都寫了程式。

- 全部只用 Python 標準函式庫，都沒有用到隨機數，每支都在 0.1 秒內跑完（本機 Python 3.9.19）。
- 輸入數字都由程式直接從 `.cache/text/<id>.txt` 解析，並印出所在行號，沒有手抄。
- 每支都有解析自檢，解析錯位時程式會直接中止：
  - OSWorld：列數、欄數與加總。
  - Arena：欄和與列和。
  - MT-Bench-101：每列 Avg. 等於任務平均。
  - SWE-bench：6 類相加等於 Applied。
- 沒有下載任何資料集或 repo。

| # | 主張 | 論文 | 結論 |
| --- | --- | --- | --- |
| 1 | OSWorld 一律回 FAIL 可拿 30/369 = 8.13%。Table 5 的 26 組只有 5 組高過它，純截圖最高 5.80% | [arXiv:2404.07972] | **證實**。有兩個補充：一是 Table 5 有 4 列的 Overall 和自己的分類分數兜不攏，但都離 8.13 很遠，不影響計數；二是 8.13% 是下限 |
| 2 | Arena Table 5 把欄方向比例當成真陽性率。α=0.1 的 93% 是精確率，召回率 52%、真陰性率 96%。正文「90% TP、60–70% TN」在任何 α 下都無法以標準定義同時成立 | [arXiv:2403.04132] | **證實，但範圍要收窄**。表上只有兩個 α。另外，判準若放寬到 recall 90±6，α=0.3（84%／68%）就同時成立 |
| 3 | MT-Bench-101 Table 3 平均列的 TS、CC、MR、GR 四欄與 18 列平均差約 0.59–0.60 | [arXiv:2402.14762] | **證實**。實際差距是 0.585–0.599，其餘 10 欄的差距都在 0.005 以內；只驗了快取的 arxiv-html 版 |
| 4 | SWE-bench C.5 的「F2P 零 pass 中 60%–70% 是 No-Op」與 Table 23 逐模型不符：Claude 2 51.9%、GPT-4 50.8% | [arXiv:2310.06770] | **證實**。有一個補充：5 個模型合併剛好是 60.04%，落在區間下緣 |

---

## 主張 1：OSWorld 的「一律回 FAIL」地板

**主張**：agent 第一步就回 FAIL，就能拿到 30/369 = 8.13%。Table 5 的 26 組模型×設定中，只有 5 組高過這個地板；純截圖設定最高 5.80%，全部低於它。

**出處**：[arXiv:2404.07972]，`notes/2404.07972.json` 的 `limitations_observed[0]`。章節第 71、338 行。

**方法**（[`07-osworld-fail-floor.py`](07-osworld-fail-floor.py)）：

- 從 `.cache/text/2404.07972.txt` 解析兩張表：
  - Table 10（第 3220 行起）：各應用的 Examples 與 #Infeasible。
  - Table 5（第 755–1565 行）：26 列、每列 5 類分數加 Overall。
- 引用 §2.1 第 140–141 行的 reward 定義：
  - 對 infeasible 題正確預測失敗就給分。
  - 「In all other scenarios, it returns 0」。
- 斷言 Table 10 的加總與 Overall 欄一致，也斷言 Table 5 四組設定分別是 8／6／6／6 列。
- **前提檢查**：
  - 依 Table 5 caption 的分組，從 Table 10 算出五類題數：OS 24、Office 117、Daily 78、Profess. 49、Workflow 101。
  - 對 26 列重算加權 Overall，確認 Overall 是 369 題的等權平均。只有這樣，拿它和 30/369 比才是同一個量。
- 數出 Overall 大於 30/369 的列，報告值與重算值各數一次；再取 Screenshot 組的最大值。

**實際輸出（摘要）**：

- **Table 10**：
  - Examples 加總 369，#Infeasible 加總 30，都與 Overall 欄一致。
  - 30/369 = **8.1301%**。
  - 論文自己在第 518 行寫「30 tasks or 8.1%」，Table 6 的 Infeasible「% of Total」也寫 8.13%。
- **前提檢查**：
  - 26 列中有 22 列，重算值與報告值的差在捨入上限 0.01 以內。
  - 另外 4 列表內自己兜不攏：
    - CogAgent（Screenshot + A11y tree）：報告 1.32，重算 1.221
    - Llama-3-70B（A11y tree）：報告 1.61，重算 1.692
    - GPT-4V（Screenshot）：報告 5.26，重算 5.200
    - GPT-4V（Screenshot + A11y tree）：報告 12.17，重算 12.188
  - 這 4 列都離 8.13 很遠，用重算值再數一次，名單不變。
- **高過地板的 5 列**：
  - GPT-4（A11y tree）12.24%
  - GPT-4V（Screenshot + A11y tree）12.17%
  - GPT-4V（Set-of-Mark）11.77%
  - GPT-4o（A11y tree）11.36%
  - GPT-4o（Screenshot + A11y tree）11.21%
- 地板以下最高的是 Gemini-Pro-1.5（Set-of-Mark）7.79%，比地板低 0.34 個百分點。
- **Screenshot 組**：6 列分別是 1.11、5.26、**5.80**（Gemini-ProV）、5.40、2.42、5.03，全部低於 8.13。
- **補充：分類看**：
  - Profess. 類的地板是 15/49 = 30.61%，26 列中沒有任何一列高過它，最高的是 GPT-4 的 26.53%。
  - OS 類的地板是 5/24 = 20.83%，只有 2 列高過它。

**結論：證實。**

- 30/369 = 8.13%、5 組、5.80% 三個數字都對得上。
- Overall 大致是 369 題的等權平均，所以這個比較成立。
- 有兩個補充：
  1. Table 5 有 4 列的 Overall 和自己的分類分數兜不攏，這是論文表格的另一個瑕疵，但不影響本主張的計數。
  2. 8.13% 精確說是下限。如果有 feasible 題在初始狀態就被評估腳本判成功，「一律回 FAIL」的 agent 會拿得更多。這一點要讀評估腳本才查得到，本程式無法檢查。

**重現**：`python3 .docs/chat-agent-research/verify/07-osworld-fail-floor.py`

---

## 主張 2：Chatbot Arena Table 5 的比例方向

**主張**：Table 5 把欄方向的比例當成真陽性率。α=0.1 時的 93%（13/14）其實是精確率；依標準定義，召回率只有 13/25 = 52%，真陰性率是 24/25 = 96%。正文「90% true positive、60–70% true negative」在任何 α 下都無法以標準定義同時成立。

**出處**：[arXiv:2403.04132]，`notes/2403.04132.json` 的 `limitations_observed[0]`。章節第 584 行。

**方法**（[`07-arena-anomaly-confusion.py`](07-arena-anomaly-confusion.py)）：

- 從 `.cache/text/2403.04132.txt` 取三樣東西：
  - 第 601–602 行：異常使用者 25 人、正常使用者 25 人。
  - 第 604 行正文：「reaching 90% true positive and 60-70% true negative rate」。
  - Table 5（第 607 行起）：α=0.1 與 α=0.3 兩個 2×2 表，每格寫成「分子/分母」。
- **證明分母是欄和**：
  - 每欄兩格的分母相同，而且等於該欄兩個分子相加。
  - 每列分子相加等於 25 人。
- 逐 α 重建 TP、FN、FP、TN，算 precision、recall、TNR、NPV。
- 檢查能否同時滿足兩個條件：「recall 約 90%」與「TNR 在 60–70%」。
  - 主判準照原文的 reaching：recall ≥ 90%。
  - 另外列出 recall 容差 ±5 與 ±6 個百分點的結果。

**實際輸出（摘要）**：

- **分母結構**：兩個 α 的欄和、列和檢查全部成立。
  - 以 α=0.1 為例：14 = 13+1，36 = 12+24，13+12 = 25，1+24 = 25。
  - 所以表中每格是「該格 ÷ 同一欄（同一個預測結果）的總數」。
- **正文數字的來源**：α=0.1 的欄方向比例是 13/14 = 92.9%、24/36 = 66.7%，剛好對上正文的「90% TP、60–70% TN」。α=0.3 的欄方向比例是 72.4%／81.0%，對不上。

| α | TP／FN／FP／TN | precision | recall | TNR | NPV |
| --- | --- | --- | --- | --- | --- |
| 0.1 | 13／12／1／24 | 92.9% | **52.0%** | **96.0%** | 66.7% |
| 0.3 | 21／4／8／17 | 72.4% | 84.0% | 68.0% | 81.0% |

- **同時成立的 α**：
  - recall ≥ 90：沒有。
  - recall 在 90±5：沒有。
  - recall 在 90±6：α=0.3，也就是 84%／68%。

**結論：證實，但「任何 α」要收窄為「報告的兩個 α」。**

- 「93% 是精確率、召回率 52%、真陰性率 96%」三個數字都對得上。
- 分母是欄和這一點，由表格自己的數字直接證明。
- 正文的 90%／60–70% 正是 α=0.1 的欄方向比例，也就是 precision 與 NPV。
- 在報告的兩個 α 下，照原文 reaching 90% 的讀法，沒有一個 α 能以標準定義同時成立。
- 有兩個保留：
  1. 表上只有 α=0.1 與 0.3。從 0.1 到 0.3，recall 上升、TNR 下降，所以沒報告的更大 α 有可能同時落在約 90%／60–70%。那一部分**無法判定**。
  2. 若把「約 90%」讀成 90±6，α=0.3 就同時成立。結論依賴容差怎麼讀。

**重現**：`python3 .docs/chat-agent-research/verify/07-arena-anomaly-confusion.py`

---

## 主張 3：MT-Bench-101 Table 3 的平均列

**主張**：以表身 18 列重算 Table 3 的平均列，TS、CC、MR、GR 四欄與報告值差約 0.59–0.60。例如 MR 報告 3.61、重算 3.01；CC 報告 8.24、重算 8.83。

**出處**：[arXiv:2402.14762]，`notes/2402.14762.json` 的 `limitations_observed[0]`。章節第 579 行。

**方法**（[`07-mtbench101-avg-row.py`](07-mtbench101-avg-row.py)）：

- 從 `.cache/text/2402.14762.txt` 解析 Table 3：
  - 以第 346 行的縮寫表頭（Avg. | CM | … | PI）為錨。
  - 斷言 18 個模型列，每列 14 個數；平均列在第 649 行。
- **解析自檢**：每列 Avg. 應等於該列 13 個任務的平均。
- 逐欄算 18 列的算術平均，與平均列相減，列出差距大於 0.05 的欄。
- **補充**：依報告值與重算值各排一次難易，對照第 720 行正文。原文是「content confusion and format rephrasing are relatively less difficult」。

**實際輸出（摘要）**：

- **解析自檢**：18 列的 Avg. 與任務平均的最大差是 0.0046，欄位沒有錯位。
- **對不上的欄**：

| 欄 | 報告 | 重算 | 報告 − 重算 |
| --- | --- | --- | --- |
| TS | 7.72 | 8.314 | −0.594 |
| CC | 8.24 | 8.827 | −0.587 |
| MR | 3.61 | 3.011 | +0.599 |
| GR | 4.84 | 4.255 | +0.585 |

- 其餘 10 欄（Avg.、CM、SI、AR、CR、FR、SC、SA、IC、PI）的差距都在 0.005 以內。
- 四欄的差值相加是 +0.002，正負互相抵銷，所以平均列的 Avg. 仍然一致：報告 6.92，重算 6.922。
- **難易排序**：
  - 依報告的平均列，最容易的兩項是 FR 與 CR。
  - 依重算值，最容易的兩項是 CC 與 FR。
  - 最難的都是 MR。
  - 正文說「CC 與 FR 相對容易」，這和重算值相符，和平均列不符。

**結論：證實。**

- 四欄、差距、MR 與 CC 的例子都對得上。實際差距是 0.585–0.599，四捨五入後就是主張說的「約 0.59–0.60」。
- 其餘 10 欄的一致性也證實了筆記說的「10 欄一致」。
- 版本限制：快取來源是 arxiv-html，快取的 metadata 沒有記錄版本（筆記說它依 v3）。PDF 版未核對。

**重現**：`python3 .docs/chat-agent-research/verify/07-mtbench101-avg-row.py`

---

## 主張 4：SWE-bench C.5 的 No-Op 比例

**主張**：C.5 說，未解決而且一個 F2P 都沒 pass 的案例中，60% 到 70% 是 No-Op。但依 Table 23，Claude 2 是 471/(471+436) ≈ 51.9%，GPT-4 是 30/(30+29) ≈ 50.8%；只有 ChatGPT-3.5（174/(174+90) ≈ 65.9%）與兩個 SWE-Llama 落在這個區間。

**出處**：[arXiv:2310.06770]，`notes/2310.06770.json` 的 `limitations_observed[15]`。章節第 563 行。

**方法**（[`07-swebench-noop-share.py`](07-swebench-noop-share.py)）：

- 從 `.cache/text/2310.06770.txt` 解析 Table 22（C.5，第 2537 行起）。
- 從表格結構找出「F2P = None」那一欄的類別，不寫死。結果是 No-Op（P2P 全過）與 Regression（P2P 部分過或全沒過）。
- F2P 零 pass 不可能是 Resolved，所以「未解決且 F2P 零 pass」＝ No-Op＋Regression。
- 解析 Table 23（第 2578–2634 行），斷言每個模型的 6 類相加等於 Applied。
- 對每個模型算 No-Op ÷（No-Op＋Regression），檢查是否落在 [60%, 70%]。
- **補充**：5 個模型合併的比例，以及 C.5 前一句「未解決中多數是 F2P 零 pass」的逐模型比例。

**實際輸出（摘要）**：

- **自檢**：5 個模型的 6 類相加都等於 Applied（1078、284、76、1257、1196）。

| 模型 | No-Op／(No-Op＋Regression) | 比例 | 在 60–70%？ |
| --- | --- | --- | --- |
| Claude 2 | 471／907 | **51.9%** | 否 |
| ChatGPT-3.5 | 174／264 | 65.9% | 是 |
| GPT-4\* | 30／59 | **50.8%** | 否 |
| SWE-Llama 7b | 716／1137 | 63.0% | 是 |
| SWE-Llama 13b | 672／1069 | 62.9% | 是 |

- **合併比例**：
  - 5 個模型的計數直接相加，得 2063/3436 = **60.04%**。
  - 去掉 Claude 2 後是 62.95%。
- **C.5 前一句**：「未解決中多數是 F2P 零 pass」逐模型是 89.4%–97.1%，成立。

**結論：證實。**

- 逐模型看，Claude 2 與 GPT-4 都約 51%，不在作者說的 60–70%。落在區間裡的正好是 ChatGPT-3.5 與兩個 SWE-Llama，四個比例的數字也都對得上。
- 有兩個補充：
  1. 5 個模型合併剛好是 60.04%，落在區間下緣。如果作者講的是合併值，原文勉強站得住，但原文沒說是哪一種。另外，GPT-4 只跑了 25% 子集，合併時各模型的分母並不相同。
  2. 章節同一段還提到另一項核對：「Table 23 的 Resolved 除以題數，對上 Table 18 的 oracle 分數」。它不在這次的候選主張內，沒有驗。

**重現**：`python3 .docs/chat-agent-research/verify/07-swebench-noop-share.py`
