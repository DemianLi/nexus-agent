# 把 `cli-invariant-violation.fixture.ts` 那顆一律吭聲的配套入口插到出貨清單上。
#
# `name` 是相對這個檔的路徑，載入前會轉成 file URL（#499）。模組要 `export default` 那顆
# plugin——一個 patch 條目對一顆，不是對一份清單。
#
# echo 已經在出貨清單裡（id: `echo`），所以只需要這一顆配套入口。
- insert:
    - id: noisy-invariant
      name: './cli-invariant-violation.fixture.ts'
