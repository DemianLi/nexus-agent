# 把假的 MCP server 插到出貨清單上。
#
# `name` 是相對這個檔的路徑，載入前會轉成 file URL（#499）。模組要 `export default` 那顆
# plugin——一個 patch 條目對一顆，不是對一份清單。
#
# **這裡沒有 `config`**：server 的路徑要在執行期算，patch 檔只寫得下字面值，所以那份設定
# 留在模組裡。理由寫在 `cli-mcp.fixture.ts` 的檔頭。
- insert:
    - id: mcp-fixture
      name: './cli-mcp.fixture.ts'
