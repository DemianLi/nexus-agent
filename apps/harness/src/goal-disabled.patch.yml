# 把出貨清單上的 `goal` 那一列關掉。
#
# **它存在是為了問「沒有 goal 域的時候排程器怎麼辦」**（#455）。拿掉 `--plugins` 之前，
# 那個狀態是「換掉整份清單」到達的；現在只剩這一條路——關掉那一列。
#
# `disabled: true` 與「把這一行從清單裡刪掉」不同：條目仍然拿得到 id、仍然出現在診斷裡，
# 而且其他條目的自動 id 不會位移。
- id: goal
  disabled: true
