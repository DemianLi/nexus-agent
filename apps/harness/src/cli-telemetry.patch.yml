# 把 `cli-telemetry.fixture.ts` 那顆遙測 plugin 插到出貨清單上。
#
# `name` 是相對這個檔的路徑，載入前會轉成 file URL（#499）。模組要 `export default` 那顆
# plugin——一個 patch 條目對一顆，不是對一份清單。
- insert:
    - id: telemetry-otel-manual
      name: './cli-telemetry.fixture.ts'
