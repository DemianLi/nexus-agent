# 本檔只列出必要的環境變數名稱，不放值（見 docs/standards.md 的「秘密與環境變數」）。
# 值填進同目錄的 .env —— 那個檔案已被 .gitignore 排除，不會進版控。

# NVIDIA OpenAI 相容端點（https://integrate.api.nvidia.com/v1）的 API key。
# Phase 0 的真實供應商接線用（issue #31）。缺少時 `pnpm --filter @nexus/harness run spike:live`
# 會直接失敗並說明缺哪一個，不會 fallback 到別的變數，也沒有預設值。
NVIDIA_API_KEY=
